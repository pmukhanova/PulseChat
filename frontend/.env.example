# In development Vite proxies /api and /socket.io to this address.
VITE_DEV_BACKEND_URL=http://localhost:3000

# Leave these relative when frontend is served by the bundled nginx container.
VITE_API_URL=/api
VITE_SOCKET_URL=
